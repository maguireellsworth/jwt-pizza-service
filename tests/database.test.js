const DB = require('../src/database/database');

describe("database object should", () => {
    beforeAll(() => {
        const new_DB = DB();
    })
  
    test('dummy db test', async () => {
        
    })
})